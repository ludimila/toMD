# to.MD v1.1 — Plano de Implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Modernizar o to.MD para usuários não técnicos: código reestruturado em pacote `tomd/` com testes, conversão em lote, links arrastáveis, erros em português claro, log em arquivo, licença noncommercial, CI que gera o instalador Windows sozinho e aviso de nova versão dentro do app.

**Architecture:** O monólito `converter_gui.py` (1.089 linhas) vira o pacote `tomd/` com 8 módulos pequenos; a lógica pura (formatos, web, caminhos, versões, erros) fica testável sem Docling/Qt instalados. GitHub Actions roda os testes em cada push e, em tags `v*`, builda o instalador no `windows-latest`. Um updater leve consulta a API pública de releases do GitHub no startup.

**Tech Stack:** Python ≥3.10, PySide6, Docling, requests/beautifulsoup4/html5lib, pytest, PyInstaller + Inno Setup, GitHub Actions.

**Spec:** `docs/superpowers/specs/2026-07-13-tomd-v1.1-design.md`

## Restrições globais (valem para TODAS as tasks)

- **Idioma:** todo texto visível ao usuário (labels, diálogos, mensagens de erro) é **pt-BR**. Commits em português (padrão do repositório).
- **Dependências pinadas** (exatas, verificadas no PyPI em 2026-07-13): `docling==2.112.0`, `PySide6==6.11.1`, `requests==2.34.2`, `beautifulsoup4==4.15.0`, `html5lib==1.1`; dev: `pytest==9.1.1`; build: `pyinstaller==6.21.0`. Python do CI: `3.12`. `requires-python = ">=3.10"`.
- **Testes rodam sem Docling/Qt instalados.** Nenhum teste pode importar `tomd.ui`, `tomd.theme` ou `tomd.app`. Módulos importados por testes (`formats`, `web`, `engine`, `errors`, `logs`, `updates`, `version`) não podem importar docling/requests/bs4 no nível do módulo (imports pesados ficam dentro de funções). O `tests/conftest.py` fornece um stub de `PySide6.QtCore` quando o PySide6 não está instalado.
- **Fase 1 é mover código, não reescrever:** comportamento do app idêntico (única exceção: remoção dos grupos Áudio/Vídeo, que nunca funcionaram).
- **Versão única:** `tomd/version.py` é a única fonte da versão (`__version__ = "1.1"`). App, CI e instalador leem dela.
- **Ambiente de desenvolvimento é macOS sem PySide6/docling instalados.** Verificação local de módulos Qt = `python3 -m py_compile` + suíte pytest sem regressão. O smoke test real do app acontece no Windows (tasks 16 e 19, com o usuário).
- Comando de teste local: `python3 -m pytest tests/ -v` (task 1 instala o pytest).
- **Referências a linhas de `converter_gui.py`** em tasks de mudança usam o arquivo no commit `33b2dea` (estado atual, inalterado até a task 6 apagá-lo). Copiar os blocos indicados **verbatim**, inclusive comentários.

---

## Fase 1 — Fundação (o app se comporta igual)

### Task 1: Esqueleto do pacote + pyproject + infraestrutura de testes

**Files:**
- Create: `tomd/__init__.py`
- Create: `tomd/version.py`
- Create: `pyproject.toml`
- Create: `tests/conftest.py`
- Create: `tests/test_version.py`
- Modify: `.gitignore`

**Interfaces:**
- Produces: `tomd.version.__version__: str = "1.1"`; stub de `PySide6.QtCore` (`QThread`, `Signal`, `Slot`) via conftest para todos os testes futuros; `pytest` configurado com `pythonpath = ["."]`.

- [ ] **Passo 1: Instalar o pytest localmente (se ainda não houver)**

Run: `python3 -m pytest --version || python3 -m pip install pytest`
Expected: versão do pytest impressa ao final.

- [ ] **Passo 2: Escrever o teste que falha**

Criar `tests/conftest.py`:

```python
"""Permite rodar os testes de lógica pura sem PySide6 instalado.

Os módulos tomd.engine e tomd.updates importam QThread/Signal no nível do
módulo (as classes de thread precisam da base na definição). Quando o
PySide6 real não está disponível — caso do CI de testes, que instala só
dev-deps leves — um stub mínimo entra no lugar. Os testes nunca executam
as threads, só as funções puras dos mesmos módulos.
"""
import sys

try:
    import PySide6.QtCore  # noqa: F401
except ImportError:
    from types import ModuleType

    qtcore = ModuleType("PySide6.QtCore")

    class QThread:
        def __init__(self, *args, **kwargs):
            pass

    def Signal(*args, **kwargs):
        return None

    def Slot(*args, **kwargs):
        def decorator(fn):
            return fn
        return decorator

    qtcore.QThread = QThread
    qtcore.Signal = Signal
    qtcore.Slot = Slot

    pyside = ModuleType("PySide6")
    pyside.QtCore = qtcore
    sys.modules["PySide6"] = pyside
    sys.modules["PySide6.QtCore"] = qtcore
```

Criar `tests/test_version.py`:

```python
import re

from tomd.version import __version__


def test_version_e_uma_string_numerica():
    assert re.fullmatch(r"\d+(\.\d+)+", __version__)
```

- [ ] **Passo 3: Rodar o teste para vê-lo falhar**

Run: `python3 -m pytest tests/ -v`
Expected: FAIL/ERROR com `ModuleNotFoundError: No module named 'tomd'`

- [ ] **Passo 4: Criar o pacote e o pyproject**

Criar `tomd/__init__.py`:

```python
"""to.MD — conversor de documentos para Markdown."""
```

Criar `tomd/version.py`:

```python
# Fonte única da versão: lida pelo app, pelo CI (release.yml) e pelo instalador.
__version__ = "1.1"
```

Criar `pyproject.toml`:

```toml
[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "tomd"
description = "Conversor de documentos para Markdown (Docling + PySide6)"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = [
    "docling==2.112.0",
    "PySide6==6.11.1",
    "requests==2.34.2",
    "beautifulsoup4==4.15.0",
    "html5lib==1.1",
]

[project.optional-dependencies]
dev = ["pytest==9.1.1"]

[tool.setuptools]
packages = ["tomd"]

[tool.setuptools.dynamic]
version = { attr = "tomd.version.__version__" }

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
```

Acrescentar ao final do `.gitignore`:

```
# Artefatos de teste/empacotamento Python
.pytest_cache/
*.egg-info/
```

- [ ] **Passo 5: Rodar o teste para vê-lo passar**

Run: `python3 -m pytest tests/ -v`
Expected: `1 passed`

- [ ] **Passo 6: Commit**

```bash
git add tomd/ tests/ pyproject.toml .gitignore
git commit -m "Cria o pacote tomd com versão única e infraestrutura de testes"
```

---

### Task 2: `tomd/formats.py` — formatos suportados (sem Áudio/Vídeo)

**Files:**
- Create: `tomd/formats.py`
- Test: `tests/test_formats.py`

**Interfaces:**
- Produces: `FORMAT_GROUPS: dict[str, list[str]]`, `ALL_EXTENSIONS: list[str]`, `is_supported_file(file_path: str) -> bool`, `build_file_dialog_filter() -> str`.
- Origem: `converter_gui.py:44-85`, **removendo** o grupo `"Áudio / Vídeo"` (o pipeline ASR do Docling nunca foi configurado; esses formatos nunca converteram). `"Legendas"` e `"Outros"` permanecem.

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_formats.py`:

```python
from tomd.formats import (
    ALL_EXTENSIONS,
    FORMAT_GROUPS,
    build_file_dialog_filter,
    is_supported_file,
)


def test_extensao_simples():
    assert is_supported_file("peticao.pdf")
    assert is_supported_file("contrato.docx")


def test_extensao_composta_tar_gz():
    assert is_supported_file("dados.tar.gz")
    # "gz" sozinho não está na lista — só a composta "tar.gz".
    assert not is_supported_file("arquivo.gz")


def test_maiusculas():
    assert is_supported_file("CONTRATO.PDF")
    assert is_supported_file("Planilha.XLSX")


def test_rejeita_audio_e_video():
    for name in ("gravacao.mp3", "audiencia.mp4", "som.wav", "video.mov",
                 "audio.m4a", "faixa.flac", "clipe.avi", "voz.aac", "som.ogg"):
        assert not is_supported_file(name), name
    assert "Áudio / Vídeo" not in FORMAT_GROUPS


def test_legendas_e_outros_permanecem():
    assert is_supported_file("legenda.vtt")
    assert is_supported_file("dados.json")


def test_rejeita_desconhecidos():
    assert not is_supported_file("programa.exe")
    assert not is_supported_file("sem_extensao")


def test_extensoes_compostas_vem_antes_das_simples():
    # ALL_EXTENSIONS é ordenada da mais longa para a mais curta, para que
    # "tar.gz" seja testada antes de qualquer sufixo mais curto.
    assert ALL_EXTENSIONS.index("tar.gz") < ALL_EXTENSIONS.index("gz") if "gz" in ALL_EXTENSIONS else True
    assert ALL_EXTENSIONS == sorted(ALL_EXTENSIONS, key=len, reverse=True)


def test_filtro_do_dialogo_lista_grupos():
    filtro = build_file_dialog_filter()
    assert "Todos os documentos suportados" in filtro
    assert "*.pdf" in filtro
    assert "Todos os arquivos (*)" in filtro
    assert "Áudio" not in filtro
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_formats.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.formats'`

- [ ] **Passo 3: Criar `tomd/formats.py`**

Copiar de `converter_gui.py:44-85` (constantes + duas funções, com os comentários originais), removendo a linha do grupo `"Áudio / Vídeo"`:

```python
# ──────────────────────────────────────────────────────────────────────────────
# FORMATOS SUPORTADOS PELO DOCLING
# Espelha docling.datamodel.document.FormatToExtensions. Mantido como constante
# local (em vez de importado) para não precisar carregar o docling só para isso.
# Áudio/vídeo ficam de fora: o pipeline ASR do Docling não é configurado no
# app, então esses formatos nunca converteram de verdade.
# ──────────────────────────────────────────────────────────────────────────────
FORMAT_GROUPS = {
    "PDF": ["pdf"],
    "Word": ["docx", "dotx", "docm", "dotm"],
    "PowerPoint": ["pptx", "potx", "ppsx", "pptm", "potm", "ppsm"],
    "Excel": ["xlsx", "xlsm"],
    "OpenDocument": ["odt", "ott", "ods", "ots", "odp", "otp"],
    "Imagens": ["jpg", "jpeg", "png", "tif", "tiff", "bmp", "webp"],
    "Web / Markup": ["html", "htm", "xhtml", "xml", "nxml", "xbrl", "dclg"],
    "Texto / Markdown": ["md", "txt", "text", "qmd", "rmd", "Rmd", "asciidoc", "adoc", "asc", "csv", "tex", "latex"],
    "E-mail / E-book": ["eml", "epub"],
    "Legendas": ["vtt"],
    "Outros": ["json", "tar.gz"],
}

# Extensões "compostas" (com ponto no meio) precisam ser checadas antes das simples
# ao validar um nome de arquivo, senão "tar.gz" bateria como "gz" incorretamente.
ALL_EXTENSIONS = sorted(
    {ext.lower() for exts in FORMAT_GROUPS.values() for ext in exts},
    key=len,
    reverse=True,
)


def is_supported_file(file_path: str) -> bool:
    name = file_path.lower()
    return any(name.endswith("." + ext) for ext in ALL_EXTENSIONS)


def build_file_dialog_filter() -> str:
    all_patterns = " ".join(f"*.{ext}" for ext in ALL_EXTENSIONS)
    parts = [f"Todos os documentos suportados ({all_patterns})"]
    for label, exts in FORMAT_GROUPS.items():
        patterns = " ".join(f"*.{ext}" for ext in exts)
        parts.append(f"{label} ({patterns})")
    parts.append("Todos os arquivos (*)")
    return ";;".join(parts)
```

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam.

- [ ] **Passo 5: Commit**

```bash
git add tomd/formats.py tests/test_formats.py
git commit -m "Extrai tomd/formats.py e remove os formatos de áudio/vídeo"
```

---

### Task 3: `tomd/web.py` — URLs, encoding e reparo de HTML

**Files:**
- Create: `tomd/web.py`
- Test: `tests/test_web.py`

**Interfaces:**
- Consumes: `tomd.formats.is_supported_file`.
- Produces: `is_url(text: str) -> bool`, `normalize_url(text: str) -> str`, `URL_FETCH_HEADERS: dict`, `_detect_html_encoding(raw: bytes, content_type_header: str) -> str`, `_html_needs_repair(html_text: str) -> bool`, `fetch_url_source(url: str)` (retorna um `DocumentStream` do docling; requests/bs4/engine importados só dentro dela).
- Origem: `converter_gui.py:88-97` e `converter_gui.py:117-202`.

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_web.py`:

```python
from tomd.web import _detect_html_encoding, _html_needs_repair, is_url, normalize_url


def test_is_url():
    assert is_url("https://planalto.gov.br/lei")
    assert is_url("http://exemplo.com")
    assert is_url("  HTTPS://EXEMPLO.COM  ")
    assert not is_url("planalto.gov.br")
    assert not is_url("C:\\Users\\doc.pdf")


def test_normalize_url_adiciona_https():
    assert normalize_url("planalto.gov.br") == "https://planalto.gov.br"
    assert normalize_url("  exemplo.com ") == "https://exemplo.com"


def test_normalize_url_preserva_esquema_existente():
    assert normalize_url("http://exemplo.com") == "http://exemplo.com"
    assert normalize_url("https://exemplo.com") == "https://exemplo.com"


def test_encoding_do_header():
    assert _detect_html_encoding(b"<html></html>", "text/html; charset=ISO-8859-1") == "ISO-8859-1"


def test_encoding_da_meta_tag():
    raw = b'<html><head><meta charset="windows-1252"></head><body>x</body></html>'
    assert _detect_html_encoding(raw, "text/html") == "windows-1252"


def test_encoding_utf8_valido():
    raw = "acentuação em português".encode("utf-8")
    assert _detect_html_encoding(raw, "") == "utf-8"


def test_encoding_fallback_windows_1252():
    raw = "acentuação".encode("windows-1252")  # bytes inválidos em UTF-8
    assert _detect_html_encoding(raw, "") == "windows-1252"


def test_pagina_normal_nao_precisa_de_reparo():
    html = "<html><head></head><body>" + "conteúdo " * 100 + "</body></html>"
    assert not _html_needs_repair(html)


def test_body_fechado_cedo_demais_precisa_de_reparo():
    html = "<html><body>início</body>" + "x" * 500 + "</html>"
    assert _html_needs_repair(html)


def test_pagina_sem_body_de_fechamento_nao_precisa_de_reparo():
    html = "<html><body>" + "x" * 500
    assert not _html_needs_repair(html)
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_web.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.web'`

- [ ] **Passo 3: Criar `tomd/web.py`**

Conteúdo completo (funções copiadas verbatim de `converter_gui.py:88-97` e `117-202`, com dois ajustes: `is_supported_file` vem de `tomd.formats`, e `DocumentStream` é acessado via `tomd.engine` dentro de `fetch_url_source` — o import fica lá dentro para evitar import circular no carregamento do módulo):

```python
import mimetypes
import os
import re
from io import BytesIO
from urllib.parse import urlparse

from tomd.formats import is_supported_file

_URL_RE = re.compile(r"^https?://", re.IGNORECASE)


def is_url(text: str) -> bool:
    return bool(_URL_RE.match(text.strip()))


def normalize_url(text: str) -> str:
    text = text.strip()
    return text if is_url(text) else "https://" + text
```

Em seguida, copiar verbatim de `converter_gui.py`:
- linhas 117-125 (`URL_FETCH_HEADERS`, com o comentário sobre User-Agent);
- linhas 128-142 (`_detect_html_encoding`);
- linhas 145-161 (`_html_needs_repair`);
- linhas 164-202 (`fetch_url_source`), com estas duas mudanças no corpo:
  1. logo abaixo de `from bs4 import BeautifulSoup`, adicionar:
     ```python
     from tomd import engine
     ```
  2. as duas ocorrências de `DocumentStream(name=..., stream=...)` viram
     `engine.DocumentStream(name=..., stream=...)`.

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam (nenhum teste chama `fetch_url_source`, que precisa de rede/docling).

- [ ] **Passo 5: Commit**

```bash
git add tomd/web.py tests/test_web.py
git commit -m "Extrai tomd/web.py: URLs, detecção de encoding e reparo de HTML"
```

---

### Task 4: `tomd/engine.py` — Docling, threads e caminhos

**Files:**
- Create: `tomd/engine.py`
- Test: `tests/test_engine.py`

**Interfaces:**
- Consumes: `tomd.web.is_url`, `tomd.web.fetch_url_source`; stub de `PySide6.QtCore` do conftest quando Qt não está instalado.
- Produces: `suggest_markdown_path(source: str) -> str`, `estimate_initial_duration(source: str) -> Optional[float]`, `_load_docling()`, `_get_converter()`, `converter_ready() -> bool`, `reset_converter() -> None`, `WarmupThread(QThread)`, `LoaderThread(QThread)` (sinais `finished_ok`, `failed(str)`), `ConversionWorker(QThread)` (construtor `ConversionWorker(file_path)`, sinais `progress_signal(int, int)`, `finished_signal(str)`, `error_signal(str)`), global `DocumentStream` (preenchido por `_load_docling`, lido por `tomd.web`).

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_engine.py`:

```python
import os

from tomd.engine import suggest_markdown_path


def test_arquivo_local_vira_md_na_mesma_pasta():
    source = os.path.join("pasta", "peticao.pdf")
    assert suggest_markdown_path(source) == os.path.join("pasta", "peticao.md")


def test_url_com_caminho_gera_slug():
    path = suggest_markdown_path("https://example.com/artigos/lei-teste")
    assert os.path.basename(path) == "example.com-artigos-lei-teste.md"
    assert os.path.isabs(path)


def test_url_raiz_usa_o_dominio():
    path = suggest_markdown_path("https://example.com/")
    assert os.path.basename(path) == "example.com.md"
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_engine.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.engine'`

- [ ] **Passo 3: Criar `tomd/engine.py`**

Estrutura do arquivo, na ordem abaixo. Os blocos marcados "verbatim" são copiados de `converter_gui.py` sem alteração alguma (incluindo comentários e docstrings):

```python
import os

# Precisa ser definido ANTES de importar torch/docling: os modelos de
# código/fórmula (Granite-Vision) tentam compilar código nativo com o
# torch.compile, o que exige um compilador C++ (MSVC) instalado — algo que a
# máquina do usuário não tem por padrão. Desativar o torch.compile faz esses
# modelos rodarem normalmente (só um pouco mais lento), sem exigir instalar
# as Build Tools do Visual Studio.
os.environ.setdefault("TORCHDYNAMO_DISABLE", "1")
os.environ.setdefault("TORCH_COMPILE_DISABLE", "1")

import re
import sys
import threading
import time
from typing import Optional
from urllib.parse import urlparse

from PySide6.QtCore import QThread, Signal

from tomd.web import fetch_url_source, is_url

# O Docling/Torch só é importado em segundo plano (ver _load_docling), para que a
# janela de carregamento apareça instantaneamente ao abrir o programa em vez de
# ficar vários segundos sem nenhum feedback visual enquanto essas libs pesadas carregam.
DocumentConverter = None
PdfFormatOption = None
PdfPipelineOptions = None
DocumentStream = None

_progress_callback = None
```

Depois, nesta ordem:
1. `suggest_markdown_path` — verbatim de `converter_gui.py:100-114`.
2. `_load_docling` — verbatim de `converter_gui.py:330-384`.
3. `_build_format_options` — verbatim de `converter_gui.py:387-407`.
4. Cache do conversor — verbatim de `converter_gui.py:410-430` (`_converter`, `_converter_lock`, `_get_converter` com a docstring inteira).
5. Duas funções novas (a UI não deve mexer nos privados do módulo):

```python
def converter_ready() -> bool:
    """True se os modelos já foram carregados nesta sessão (ver _get_converter)."""
    return _converter is not None


def reset_converter():
    """Descarta o conversor em cache. Usado após um cancelamento forçado:
    terminate() pode interromper a inferência no meio e deixar estado
    corrompido, que contaminaria as conversões seguintes."""
    global _converter
    with _converter_lock:
        _converter = None
```

6. `WarmupThread` — verbatim de `converter_gui.py:433-450`.
7. `estimate_initial_duration` — verbatim de `converter_gui.py:453-481`.
8. `LoaderThread` — verbatim de `converter_gui.py:484-494`.
9. `ConversionWorker` — verbatim de `converter_gui.py:500-537`.

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam (o stub do conftest cobre `QThread`/`Signal`; docling nunca importa).

- [ ] **Passo 5: Commit**

```bash
git add tomd/engine.py tests/test_engine.py
git commit -m "Extrai tomd/engine.py: Docling preguiçoso, threads e caminhos de saída"
```

---

### Task 5: `tomd/theme.py` — identidade visual

**Files:**
- Create: `tomd/theme.py`

**Interfaces:**
- Produces: tokens `CANVAS, SURFACE, INK, INK_MUTED, INK_FAINT, HAIRLINE, PRIMARY, PRIMARY_ACTIVE, ACCENT_GREEN, FONT_FAMILY` (strings), `make_font(size, weight=QFont.Normal, tracking=0.0) -> QFont`, `render_app_icon() -> QPixmap`, `render_doc_glyph(color: str) -> QPixmap`, `apply_card_shadow(widget)`, `BUTTON_QSS: str`.

- [ ] **Passo 1: Criar `tomd/theme.py`**

Cabeçalho de imports:

```python
from PySide6.QtCore import Qt
from PySide6.QtGui import QColor, QFont, QPainter, QPen, QPixmap
from PySide6.QtWidgets import QGraphicsDropShadowEffect, QWidget
```

Depois, copiar verbatim de `converter_gui.py`:
- linhas 215-320 (bloco "IDENTIDADE VISUAL": tokens de cor, `FONT_FAMILY`, `make_font`, `_app_icon_cache`, `render_app_icon`, `render_doc_glyph`, `apply_card_shadow` — com todos os comentários);
- linhas 639-679 (`BUTTON_QSS`).

Não há teste automatizado (é código Qt puro); a verificação é sintática.

- [ ] **Passo 2: Verificar sintaxe e não-regressão**

Run: `python3 -m py_compile tomd/theme.py && python3 -m pytest tests/ -q`
Expected: sem erro de compilação; todos os testes passam.

- [ ] **Passo 3: Commit**

```bash
git add tomd/theme.py
git commit -m "Extrai tomd/theme.py: tokens visuais, ícones e QSS"
```

---

### Task 6: `tomd/ui.py` + `tomd/app.py` + `run.py` — troca do ponto de entrada e limpeza

**Files:**
- Create: `tomd/ui.py`
- Create: `tomd/app.py`
- Create: `run.py`
- Modify: `to.MD.spec:57`
- Modify: `README.md` (seções "Rodar do código-fonte" e "Estrutura do projeto")
- Delete: `converter_gui.py`, `converter.py`, `testar_docling.py`, `Iniciar_Conversor.vbs`

**Interfaces:**
- Consumes: tudo das tasks 2-5 (`tomd.formats.build_file_dialog_filter/is_supported_file`, `tomd.web.is_url/normalize_url`, `tomd.engine.ConversionWorker/estimate_initial_duration/suggest_markdown_path/converter_ready/reset_converter/LoaderThread/WarmupThread`, todos os nomes de `tomd.theme`).
- Produces: `tomd.ui.DropZone` (sinal `file_dropped(str)`), `tomd.ui.MainWindow`, `tomd.ui.LoadingWindow`, `tomd.ui.center_on_screen(widget)`, `tomd.app.main()`.

- [ ] **Passo 1: Criar `tomd/ui.py`**

Cabeçalho de imports:

```python
import os
import time

from PySide6.QtCore import Qt, QTimer, Signal, Slot
from PySide6.QtGui import QDragEnterEvent, QDropEvent, QFont, QIcon
from PySide6.QtWidgets import (
    QApplication,
    QFileDialog,
    QHBoxLayout,
    QLabel,
    QLineEdit,
    QMainWindow,
    QMessageBox,
    QProgressBar,
    QPushButton,
    QVBoxLayout,
    QWidget,
)

from tomd import engine
from tomd.engine import ConversionWorker, estimate_initial_duration, suggest_markdown_path
from tomd.formats import build_file_dialog_filter, is_supported_file
from tomd.theme import (
    ACCENT_GREEN,
    BUTTON_QSS,
    CANVAS,
    FONT_FAMILY,
    HAIRLINE,
    INK,
    INK_FAINT,
    INK_MUTED,
    PRIMARY,
    PRIMARY_ACTIVE,
    SURFACE,
    apply_card_shadow,
    make_font,
    render_app_icon,
    render_doc_glyph,
)
from tomd.web import is_url, normalize_url
```

Depois, copiar verbatim de `converter_gui.py`, nesta ordem:
- linhas 205-212 (`center_on_screen`);
- linhas 540-637 (comentário de seção + classe `DropZone`);
- linhas 682-1003 (comentário de seção + classe `MainWindow`), com **exatamente duas mudanças**:
  1. Em `start_conversion`, a linha `if _converter is None:` vira:
     ```python
     if not engine.converter_ready():
     ```
  2. O método `cancel_conversion` inteiro vira (mesma lógica, sem tocar em globals de outro módulo):
     ```python
     def cancel_conversion(self):
         if self.worker and self.worker.isRunning():
             self.worker.terminate()
             self.worker.wait()
             # terminate() mata a thread à força, podendo interromper o
             # modelo de OCR/tabela no meio da inferência. Como o conversor é
             # reaproveitado entre conversões, um estado corrompido aqui
             # contaminaria todas as conversões seguintes — descarta o cache
             # para a próxima reconstruir do zero, limpa.
             engine.reset_converter()
         self.elapsed_ticker.stop()
         self._conversion_start = None
         self.progress_widget.hide()
         self.file_label.setText("")
         self.drop_zone.show()
         self.url_widget.show()
     ```
- linhas 1006-1049 (comentário de seção + classe `LoadingWindow`).

- [ ] **Passo 2: Criar `tomd/app.py`**

```python
import sys

from PySide6.QtWidgets import QApplication, QMessageBox

from tomd.engine import LoaderThread, WarmupThread
from tomd.ui import LoadingWindow, MainWindow, center_on_screen


def main():
    app = QApplication(sys.argv)

    loading = LoadingWindow()
    loading.show()
    center_on_screen(loading)
    app.processEvents()  # força a janela a pintar antes de travar no import pesado

    holder = {}

    def on_docling_ready():
        window = MainWindow()
        holder["window"] = window
        window.show()
        center_on_screen(window)
        loading.close()
        # Começa a carregar os modelos agora, enquanto o usuário ainda está
        # escolhendo o arquivo — a 1ª conversão fica bem mais rápida.
        holder["warmup"] = WarmupThread()
        holder["warmup"].start()

    def on_docling_failed(message):
        QMessageBox.critical(
            loading,
            "Erro ao iniciar",
            f"Não foi possível carregar o Docling:\n{message}"
        )
        app.quit()

    loader = LoaderThread()
    loader.finished_ok.connect(on_docling_ready)
    loader.failed.connect(on_docling_failed)
    loader.start()

    sys.exit(app.exec())
```

- [ ] **Passo 3: Criar `run.py`**

```python
from tomd.app import main

if __name__ == "__main__":
    main()
```

- [ ] **Passo 4: Apontar o PyInstaller para `run.py`**

Em `to.MD.spec` (linha 57), trocar:

```python
    ['converter_gui.py'],
```

por:

```python
    ['run.py'],
```

- [ ] **Passo 5: Apagar os arquivos mortos**

```bash
git rm converter_gui.py converter.py testar_docling.py Iniciar_Conversor.vbs
```

- [ ] **Passo 6: Atualizar o README**

Na seção "Opção 2 — Rodar do código-fonte", trocar o bloco de código e o parágrafo do `.vbs` por:

````markdown
```bash
pip install -e .
python run.py
```
````

(remover a frase sobre `Iniciar_Conversor.vbs`).

Na seção "Estrutura do projeto", trocar o bloco de árvore por:

````markdown
```
toMD/
├── run.py            # 🚀 Ponto de entrada (python run.py)
├── tomd/             # 💻 O aplicativo
│   ├── app.py        #    bootstrap (janela de carregamento + janela principal)
│   ├── ui.py         #    interface Qt (PySide6)
│   ├── engine.py     #    conversão via Docling (carregado em segundo plano)
│   ├── web.py        #    download e reparo de páginas da web
│   ├── formats.py    #    formatos aceitos
│   ├── theme.py      #    identidade visual
│   └── version.py    #    versão do app (fonte única)
├── tests/            # ✅ Testes (pytest)
├── to.MD.spec        # 📦 Receita do PyInstaller (gera o to.MD.exe)
├── installer.iss     # 🎁 Receita do Inno Setup (gera o instalador)
└── app_icon.ico      # 🎨 Ícone do aplicativo
```
````

- [ ] **Passo 7: Verificar**

Run: `python3 -m py_compile run.py tomd/ui.py tomd/app.py && python3 -m pytest tests/ -q`
Expected: sem erro de compilação; todos os testes passam.

- [ ] **Passo 8: Commit**

```bash
git add -A
git commit -m "Troca converter_gui.py pelo pacote tomd/ com entrada em run.py"
```

> ⚠️ **Checkpoint da Fase 1:** a partir daqui o app só roda via `python run.py`. Como esta máquina (macOS) não tem PySide6/docling, o smoke test manual acontece no Windows antes do release (task 16). Nada nesta fase mudou comportamento — só endereços de código.

---

## Fase 2 — UX para usuários não técnicos

### Task 7: `tomd/logs.py` — log em arquivo

**Files:**
- Create: `tomd/logs.py`
- Modify: `tomd/app.py` (início de `main()`)
- Modify: `tomd/engine.py` (`ConversionWorker.run` ganha logging)
- Test: `tests/test_logs.py`

**Interfaces:**
- Produces: `log_dir() -> Path`, `log_file() -> Path`, `setup_logging() -> None` (idempotente; RotatingFileHandler ~1 MB × 3). `log_file()` será usado pelo diálogo de erro na task 8.

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_logs.py`:

```python
import logging
import os
from pathlib import Path

from tomd.logs import log_dir, log_file, setup_logging


def test_log_dir_usa_localappdata_no_windows(monkeypatch, tmp_path):
    monkeypatch.setenv("LOCALAPPDATA", str(tmp_path))
    assert log_dir() == tmp_path / "toMD" / "logs"
    assert log_file() == tmp_path / "toMD" / "logs" / "tomd.log"


def test_log_dir_tem_fallback_fora_do_windows(monkeypatch):
    monkeypatch.delenv("LOCALAPPDATA", raising=False)
    assert log_dir() == Path.home() / ".tomd" / "logs"


def test_setup_logging_escreve_no_arquivo(monkeypatch, tmp_path):
    monkeypatch.setenv("LOCALAPPDATA", str(tmp_path))
    root = logging.getLogger()
    handlers_antes = list(root.handlers)
    setup_logging()
    try:
        logging.getLogger("tomd.teste").info("olá, log")
        for h in root.handlers:
            h.flush()
        conteudo = (tmp_path / "toMD" / "logs" / "tomd.log").read_text(encoding="utf-8")
        assert "olá, log" in conteudo
    finally:
        for h in root.handlers[len(handlers_antes):]:
            root.removeHandler(h)
            h.close()
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_logs.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.logs'`

- [ ] **Passo 3: Criar `tomd/logs.py`**

```python
"""Log em arquivo para diagnóstico pós-morte: quando um advogado reporta
"não funcionou", o log é a única testemunha do que aconteceu na máquina dele."""
import logging
import os
from logging.handlers import RotatingFileHandler
from pathlib import Path

_configured = False


def log_dir() -> Path:
    base = os.environ.get("LOCALAPPDATA")
    if base:
        return Path(base) / "toMD" / "logs"
    # Fora do Windows (desenvolvimento em macOS/Linux) não existe LOCALAPPDATA.
    return Path.home() / ".tomd" / "logs"


def log_file() -> Path:
    return log_dir() / "tomd.log"


def setup_logging() -> None:
    global _configured
    if _configured:
        return
    path = log_file()
    path.parent.mkdir(parents=True, exist_ok=True)
    handler = RotatingFileHandler(path, maxBytes=1_000_000, backupCount=3, encoding="utf-8")
    handler.setFormatter(logging.Formatter("%(asctime)s %(levelname)s %(name)s: %(message)s"))
    root = logging.getLogger()
    root.setLevel(logging.INFO)
    root.addHandler(handler)
    _configured = True
```

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam. (Nota: o teste de idempotência é o guard `_configured`; `setup_logging` chamado 2× não duplica handlers.)

- [ ] **Passo 5: Ligar o log no app e no worker**

Em `tomd/app.py`, o topo do arquivo ganha:

```python
import logging
import sys

from PySide6.QtWidgets import QApplication, QMessageBox

from tomd.engine import LoaderThread, WarmupThread
from tomd.logs import setup_logging
from tomd.ui import LoadingWindow, MainWindow, center_on_screen
from tomd.version import __version__
```

e `main()` passa a começar com:

```python
def main():
    setup_logging()
    logging.getLogger("tomd").info("to.MD %s iniciando", __version__)
    app = QApplication(sys.argv)
```

Em `tomd/engine.py`: adicionar `import logging` junto aos imports do topo e, logo abaixo deles, `log = logging.getLogger(__name__)`. O método `ConversionWorker.run` passa a registrar início, duração e falha — corpo completo novo:

```python
    def run(self):
        global _progress_callback

        def progress_bridge(current, total):
            self.progress_signal.emit(current, total)

        _progress_callback = progress_bridge
        start = time.time()
        log.info("Conversão iniciada: %s", self.file_path)

        try:
            if not is_url(self.file_path) and not os.path.isfile(self.file_path):
                raise FileNotFoundError(
                    f"Não encontrei o arquivo em:\n{self.file_path}\n\n"
                    "Se ele estiver no OneDrive ou em outra nuvem, abra a pasta "
                    "no Explorador de Arquivos e espere baixar antes de arrastar."
                )

            conversor = _get_converter()

            if is_url(self.file_path):
                source = fetch_url_source(self.file_path)
            else:
                source = self.file_path
            resultado = conversor.convert(source)
            texto_markdown = resultado.document.export_to_markdown()
            log.info("Conversão concluída em %.1fs: %s", time.time() - start, self.file_path)
            self.finished_signal.emit(texto_markdown)
        except Exception as e:
            log.exception("Conversão falhou: %s", self.file_path)
            self.error_signal.emit(str(e))
        finally:
            _progress_callback = None
```

- [ ] **Passo 6: Verificar e commitar**

Run: `python3 -m pytest tests/ -q && python3 -m py_compile tomd/app.py tomd/engine.py`
Expected: todos passam; sem erro de compilação.

```bash
git add tomd/logs.py tomd/app.py tomd/engine.py tests/test_logs.py
git commit -m "Log em arquivo (LOCALAPPDATA/toMD/logs) com rotação e registro de conversões"
```

---

### Task 8: `tomd/errors.py` — erros em português claro

**Files:**
- Create: `tomd/errors.py`
- Modify: `tomd/engine.py` (`ConversionWorker` emite a exceção, não `str`)
- Modify: `tomd/ui.py` (`on_error` e `_prompt_save` usam as mensagens amigáveis)
- Test: `tests/test_errors.py`

**Interfaces:**
- Consumes: `tomd.logs.log_file` (o diálogo menciona onde fica o log).
- Produces: `UserFacingError(Exception)` (mensagem já pronta para o usuário), `friendly_error(exc: BaseException) -> str`, `friendly_save_error(exc: BaseException, folder: str) -> str`, `GENERIC_MESSAGE: str`. **Muda a interface do worker:** `ConversionWorker.error_signal` passa de `Signal(str)` para `Signal(object)` e emite o objeto da exceção.

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_errors.py`:

```python
from types import SimpleNamespace

from tomd.errors import (
    GENERIC_MESSAGE,
    UserFacingError,
    friendly_error,
    friendly_save_error,
)


def test_pdf_com_senha():
    exc = RuntimeError("Failed to open PDF: password required (pypdfium2)")
    assert friendly_error(exc) == (
        "Este PDF está protegido por senha. Remova a senha e tente de novo."
    )


def test_sem_conexao_por_classe():
    exc = ConnectionError("connection aborted")
    assert friendly_error(exc) == (
        "Sem conexão com a internet — verifique sua rede e tente de novo."
    )


def test_falha_de_dns_pela_mensagem():
    exc = Exception("HTTPSConnectionPool: Max retries exceeded (getaddrinfo failed)")
    assert friendly_error(exc) == (
        "Sem conexão com a internet — verifique sua rede e tente de novo."
    )


def test_http_4xx_5xx_mostra_o_codigo():
    HTTPError = type("HTTPError", (Exception,), {})
    exc = HTTPError("403 Client Error: Forbidden for url")
    exc.response = SimpleNamespace(status_code=403)
    assert friendly_error(exc) == "O site não permitiu baixar esta página (erro 403)."


def test_arquivo_corrompido():
    ConversionError = type("ConversionError", (Exception,), {})
    exc = ConversionError("File format not recognized")
    assert friendly_error(exc) == (
        "Não consegui ler este arquivo — ele pode estar corrompido."
    )


def test_causa_encadeada_e_inspecionada():
    outer = RuntimeError("conversion failed")
    outer.__cause__ = ConnectionError("network unreachable")
    assert friendly_error(outer) == (
        "Sem conexão com a internet — verifique sua rede e tente de novo."
    )


def test_user_facing_error_passa_direto():
    assert friendly_error(UserFacingError("mensagem já pronta")) == "mensagem já pronta"


def test_desconhecido_cai_na_generica():
    assert friendly_error(ValueError("boom")) == GENERIC_MESSAGE


def test_erro_ao_salvar_menciona_a_pasta():
    msg = friendly_save_error(PermissionError("denied"), "/uma/pasta")
    assert msg == "Não consegui salvar em /uma/pasta — escolha outra pasta."
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_errors.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.errors'`

- [ ] **Passo 3: Criar `tomd/errors.py`**

```python
"""Tradução de erros técnicos para mensagens em português claro.

As regras casam por NOME de classe (não por isinstance) de propósito: as
exceções vêm de libs pesadas (requests, docling, pypdfium2) que este módulo
não pode importar — os testes rodam sem elas instaladas.
"""

GENERIC_MESSAGE = (
    "Não consegui converter este documento. Tente de novo; se o problema "
    "continuar, os detalhes técnicos abaixo ajudam a entender o motivo."
)


class UserFacingError(Exception):
    """Erro cuja mensagem já foi escrita para o usuário final (sem tradução)."""


def _iter_chain(exc):
    seen = set()
    while exc is not None and id(exc) not in seen:
        seen.add(id(exc))
        yield exc
        exc = exc.__cause__ or exc.__context__


def _has_class(exc, *names):
    for e in _iter_chain(exc):
        for cls in type(e).__mro__:
            if cls.__name__ in names:
                return True
    return False


def _http_status(exc):
    for e in _iter_chain(exc):
        status = getattr(getattr(e, "response", None), "status_code", None)
        if isinstance(status, int):
            return status
    return None


def friendly_error(exc: BaseException) -> str:
    """Mensagem humana para o erro; casos desconhecidos caem na genérica."""
    if isinstance(exc, UserFacingError):
        return str(exc)

    full_text = " ".join(str(e) for e in _iter_chain(exc)).lower()

    if "password" in full_text or "encrypted" in full_text:
        return "Este PDF está protegido por senha. Remova a senha e tente de novo."

    status = _http_status(exc)
    if _has_class(exc, "HTTPError") and status:
        return f"O site não permitiu baixar esta página (erro {status})."

    if (
        _has_class(exc, "ConnectionError", "ConnectTimeout", "ReadTimeout", "Timeout")
        or "getaddrinfo" in full_text
        or "name or service not known" in full_text
        or "nodename nor servname" in full_text
    ):
        return "Sem conexão com a internet — verifique sua rede e tente de novo."

    if _has_class(exc, "ConversionError") or "corrupt" in full_text or "not valid" in full_text:
        return "Não consegui ler este arquivo — ele pode estar corrompido."

    return GENERIC_MESSAGE


def friendly_save_error(exc: BaseException, folder: str) -> str:
    """Erro de disco/permissão na hora de salvar o .md."""
    return f"Não consegui salvar em {folder} — escolha outra pasta."
```

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam.

- [ ] **Passo 5: Worker emite a exceção; diálogos usam as mensagens amigáveis**

Em `tomd/engine.py`:
1. Adicionar aos imports: `from tomd.errors import UserFacingError`.
2. Em `ConversionWorker`: `error_signal = Signal(object)` (era `Signal(str)`).
3. No `run()`, o `raise FileNotFoundError(...)` vira `raise UserFacingError(...)` (mesma mensagem, que já está em português).
4. Ainda no `run()`, `self.error_signal.emit(str(e))` vira `self.error_signal.emit(e)`.

Em `tomd/ui.py`:
1. Adicionar aos imports:
   ```python
   import traceback

   from tomd.errors import friendly_error, friendly_save_error
   from tomd.logs import log_file
   ```
2. Substituir `on_error` inteiro:
   ```python
   def on_error(self, exc):
       self.elapsed_ticker.stop()
       self._conversion_start = None
       box = QMessageBox(self)
       box.setIcon(QMessageBox.Critical)
       box.setWindowTitle("Erro de conversão")
       box.setText(friendly_error(exc))
       box.setInformativeText(
           f"Se precisar de ajuda, o registro completo está em:\n{log_file()}"
       )
       # Detalhe técnico colapsado: o Qt só mostra ao clicar em "Show Details…".
       box.setDetailedText("".join(traceback.format_exception(exc)))
       box.exec()
       self.progress_widget.hide()
       self.file_label.setText("")
       self.drop_zone.show()
       self.url_widget.show()
   ```
3. Em `_prompt_save`, o bloco `except Exception as e:` vira:
   ```python
           except Exception as e:
               QMessageBox.critical(
                   self,
                   "Erro ao salvar",
                   friendly_save_error(e, os.path.dirname(save_path)),
               )
   ```

- [ ] **Passo 6: Verificar e commitar**

Run: `python3 -m pytest tests/ -q && python3 -m py_compile tomd/engine.py tomd/ui.py`
Expected: todos passam; sem erro de compilação.

```bash
git add tomd/errors.py tomd/engine.py tomd/ui.py tests/test_errors.py
git commit -m "Erros em português claro, com detalhe técnico colapsado e caminho do log"
```

---

### Task 9: Memória de pasta (QSettings)

**Files:**
- Modify: `tomd/ui.py` (DropZone.open_file_dialog, MainWindow._prompt_save)

**Interfaces:**
- Produces: helper de módulo `_settings() -> QSettings` em `tomd/ui.py` com chave `"last_dir"` (a task 10 reutiliza para a pasta de saída do lote).

- [ ] **Passo 1: Adicionar o helper**

Em `tomd/ui.py`: adicionar `QSettings` ao import de `PySide6.QtCore` (fica `from PySide6.QtCore import QSettings, Qt, QTimer, Slot`) e, logo após os imports:

```python
def _settings() -> QSettings:
    # Escopo organização/app fixo: a mesma memória vale para abrir e salvar.
    return QSettings("toMD", "toMD")
```

- [ ] **Passo 2: Lembrar a pasta ao abrir arquivo**

`DropZone.open_file_dialog` vira:

```python
    def open_file_dialog(self):
        settings = _settings()
        start_dir = settings.value("last_dir", "") or ""
        file_path, _ = QFileDialog.getOpenFileName(
            self, "Selecionar documento", start_dir, build_file_dialog_filter()
        )
        if file_path:
            settings.setValue("last_dir", os.path.dirname(file_path))
            self.file_dropped.emit(file_path)
```

- [ ] **Passo 3: Lembrar a pasta ao salvar**

Em `MainWindow._prompt_save`, o início do método vira:

```python
    def _prompt_save(self, original_path, md_content):
        suggested_path = suggest_markdown_path(original_path)
        settings = _settings()
        last_dir = settings.value("last_dir", "") or ""
        # Arquivo local sugere a própria pasta de origem (mais útil); para
        # URLs, que não têm pasta, a última pasta usada vence o padrão
        # Documentos.
        if is_url(original_path) and last_dir:
            suggested_path = os.path.join(last_dir, os.path.basename(suggested_path))

        save_path, _ = QFileDialog.getSaveFileName(
            self,
            "Salvar arquivo Markdown",
            suggested_path,
            "Arquivos Markdown (*.md)"
        )
```

e, dentro do `if save_path:`, a primeira linha passa a ser:

```python
        if save_path:
            settings.setValue("last_dir", os.path.dirname(save_path))
            try:
```

(o resto do método permanece igual).

- [ ] **Passo 4: Verificar e commitar**

Run: `python3 -m py_compile tomd/ui.py && python3 -m pytest tests/ -q`
Expected: sem erro; todos passam.

```bash
git add tomd/ui.py
git commit -m "Lembra a última pasta usada entre sessões (QSettings)"
```

---

### Task 10: Conversão em lote

**Files:**
- Modify: `tomd/engine.py` (nova função `unique_path`)
- Modify: `tomd/ui.py` (DropZone multi-arquivo; MainWindow ganha fila, resumo e cancelamento de fila)
- Test: `tests/test_engine.py` (casos de `unique_path`)

**Interfaces:**
- Consumes: `tomd.errors.friendly_error/friendly_save_error`, `_settings()` da task 9.
- Produces: `tomd.engine.unique_path(path: str) -> str`; **muda a interface do DropZone:** o sinal `file_dropped = Signal(str)` é substituído por `files_dropped = Signal(list)` (lista de caminhos); `MainWindow.start_sources(paths: list[str])` é o novo ponto de entrada de conversões de arquivos (a task 11 continua usando `start_conversion(source: str)` para URLs).

- [ ] **Passo 1: Escrever os testes de `unique_path` (falham)**

Acrescentar ao final de `tests/test_engine.py`:

```python
from tomd.engine import unique_path


def test_unique_path_devolve_o_original_se_livre(tmp_path):
    target = str(tmp_path / "doc.md")
    assert unique_path(target) == target


def test_unique_path_adiciona_sufixo_em_conflito(tmp_path):
    (tmp_path / "doc.md").write_text("x")
    assert unique_path(str(tmp_path / "doc.md")) == str(tmp_path / "doc (2).md")


def test_unique_path_pula_sufixos_ja_existentes(tmp_path):
    (tmp_path / "doc.md").write_text("x")
    (tmp_path / "doc (2).md").write_text("x")
    assert unique_path(str(tmp_path / "doc.md")) == str(tmp_path / "doc (3).md")
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_engine.py -v`
Expected: FAIL com `ImportError: cannot import name 'unique_path'`

- [ ] **Passo 3: Implementar `unique_path` em `tomd/engine.py`**

Logo após `suggest_markdown_path`:

```python
def unique_path(path: str) -> str:
    """Primeiro caminho livre entre nome.md, nome (2).md, nome (3).md…
    Usado no lote, onde salvar é automático e sobrescrever seria destrutivo."""
    if not os.path.exists(path):
        return path
    base, ext = os.path.splitext(path)
    n = 2
    while os.path.exists(f"{base} ({n}){ext}"):
        n += 1
    return f"{base} ({n}){ext}"
```

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam.

- [ ] **Passo 5: DropZone passa a entregar todos os arquivos soltos**

Em `tomd/ui.py`, classe `DropZone`:
1. `file_dropped = Signal(str)` vira `files_dropped = Signal(list)`.
2. Em `open_file_dialog`, `self.file_dropped.emit(file_path)` vira `self.files_dropped.emit([file_path])`.
3. `dropEvent` inteiro vira:
   ```python
   def dropEvent(self, event: QDropEvent):
       self.reset_style()
       event.acceptProposedAction()
       files = [
           url.toLocalFile()
           for url in event.mimeData().urls()
           if url.toLocalFile() and is_supported_file(url.toLocalFile())
       ]
       if files:
           self.files_dropped.emit(files)
           return
       QMessageBox.warning(
           self,
           "Formato não suportado",
           "Esse tipo de arquivo não é reconhecido pelo conversor.\n"
           "Veja a lista de formatos aceitos em \"Escolher arquivo\"."
       )
   ```

- [ ] **Passo 6: MainWindow ganha a fila de lote**

Em `tomd/ui.py`, classe `MainWindow`:

1. No `__init__`, depois de `self.worker = None`, adicionar:
   ```python
   self._batch_queue = None   # lista de arquivos do lote (None = conversão avulsa)
   self._batch_dir = None
   self._batch_index = 0
   self._batch_results = []   # tuplas (origem, sucesso: bool, info: str)
   ```
2. Em `init_ui`, trocar `self.drop_zone.file_dropped.connect(self.start_conversion)` por `self.drop_zone.files_dropped.connect(self.start_sources)`.
3. Adicionar aos imports do arquivo: `from tomd.engine import unique_path` (juntar ao import existente de `tomd.engine`) e `import logging`; logo após os imports, `log = logging.getLogger(__name__)`.
4. Substituir `start_conversion` e adicionar os métodos novos (o corpo de `_start_worker` é o antigo `start_conversion`, com o rótulo de lote e as conexões novas):

```python
    def start_sources(self, paths):
        """Um arquivo mantém o fluxo atual ("Salvar como"); dois ou mais
        entram na fila de lote, com uma única escolha de pasta de saída."""
        if len(paths) == 1:
            self.start_conversion(paths[0])
            return
        settings = _settings()
        start_dir = settings.value("last_dir", "") or ""
        out_dir = QFileDialog.getExistingDirectory(
            self, "Escolha a pasta onde salvar os arquivos .md", start_dir
        )
        if not out_dir:
            return
        settings.setValue("last_dir", out_dir)
        self._batch_queue = list(paths)
        self._batch_dir = out_dir
        self._batch_index = 0
        self._batch_results = []
        self._start_next_in_batch()

    def start_conversion(self, source):
        self._batch_queue = None
        self._start_worker(source)

    def _start_next_in_batch(self):
        if self._batch_index >= len(self._batch_queue):
            self._finish_batch()
            return
        self._start_worker(self._batch_queue[self._batch_index])

    def _start_worker(self, source):
        self.drop_zone.hide()
        self.url_widget.hide()
        label = source if is_url(source) else os.path.basename(source)
        if self._batch_queue is not None:
            label = f"arquivo {self._batch_index + 1} de {len(self._batch_queue)} — {label}"
        self.file_label.setText(label)
        self.progress_bar.setValue(0)
        if not engine.converter_ready():
            self.status_label.setText("Carregando modelos (pode demorar mais na 1ª vez)…")
        else:
            self.status_label.setText("Preparando conversão…")
        self.progress_widget.show()

        self._conversion_start = time.time()
        self._estimated_total = estimate_initial_duration(source)
        self._update_time_label()
        self.elapsed_ticker.start()

        self.worker = ConversionWorker(source)
        self.worker.progress_signal.connect(self.update_progress)
        self.worker.finished_signal.connect(lambda md: self._on_worker_finished(source, md))
        self.worker.error_signal.connect(lambda exc: self._on_worker_error(source, exc))
        self.worker.start()

    def _on_worker_finished(self, source, md_content):
        if self._batch_queue is None:
            self.on_success(source, md_content)
            return
        name = os.path.splitext(os.path.basename(source))[0] + ".md"
        target = unique_path(os.path.join(self._batch_dir, name))
        try:
            with open(target, "w", encoding="utf-8") as f:
                f.write(md_content)
            self._batch_results.append((source, True, os.path.basename(target)))
        except OSError as e:
            log.exception("Falha ao salvar %s", target)
            self._batch_results.append((source, False, friendly_save_error(e, self._batch_dir)))
        self._batch_index += 1
        self._start_next_in_batch()

    def _on_worker_error(self, source, exc):
        if self._batch_queue is None:
            self.on_error(exc)
            return
        self._batch_results.append((source, False, friendly_error(exc)))
        self._batch_index += 1
        self._start_next_in_batch()

    def _finish_batch(self):
        self.elapsed_ticker.stop()
        self._conversion_start = None
        self.progress_widget.hide()
        self.file_label.setText("")
        self.drop_zone.show()
        self.url_widget.show()

        results = self._batch_results
        out_dir = self._batch_dir
        self._batch_queue = None
        ok = [r for r in results if r[1]]
        failed = [r for r in results if not r[1]]
        lines = [f"Convertidos com sucesso: {len(ok)} de {len(results)}."]
        if failed:
            lines.append("")
            lines.append("Falharam:")
            for source, _, reason in failed:
                lines.append(f"• {os.path.basename(source)} — {reason}")
        lines.append("")
        lines.append(f"Os arquivos .md estão em:\n{out_dir}")
        QMessageBox.information(self, "Conversão em lote concluída", "\n".join(lines))
```

5. Em `update_progress`, o início do método vira (o refinamento de estimativa no final permanece igual):
   ```python
   @Slot(int, int)
   def update_progress(self, current, total):
       prefix = ""
       if self._batch_queue is not None:
           prefix = f"arquivo {self._batch_index + 1} de {len(self._batch_queue)} · "
       if total > 1:
           page_text = f"página {current} de {total}"
       else:
           page_text = "processando documento"
       if prefix:
           self.status_label.setText(f"{prefix}{page_text}…")
       else:
           self.status_label.setText(f"{page_text.capitalize()}…")
       percent = int((current / total) * 100) if total > 0 else 0
       self.progress_bar.setValue(percent)

       if total > 1 and current > 0 and self._conversion_start:
           elapsed = time.time() - self._conversion_start
           self._estimated_total = elapsed * total / current
   ```
6. Em `cancel_conversion`, a primeira linha do método passa a ser:
   ```python
   self._batch_queue = None  # cancelar interrompe a fila inteira, não só o arquivo atual
   ```

- [ ] **Passo 7: Verificar e commitar**

Run: `python3 -m py_compile tomd/ui.py tomd/engine.py && python3 -m pytest tests/ -q`
Expected: sem erro; todos passam.

```bash
git add tomd/ui.py tomd/engine.py tests/test_engine.py
git commit -m "Conversão em lote: fila com pasta única, sufixos anticonflito e resumo final"
```

---

### Task 11: Arrastar links do navegador

**Files:**
- Modify: `tomd/ui.py` (DropZone aceita URLs http/https; MainWindow conecta o sinal novo)

**Interfaces:**
- Consumes: `MainWindow.start_conversion(source: str)` (já aceita URLs — mesmo caminho do campo de texto), `tomd.web.is_url`.
- Produces: `DropZone.url_dropped = Signal(str)` e helper `DropZone._split_payload(mime_data) -> tuple[list, list]`.

- [ ] **Passo 1: Aceitar links no DropZone**

Em `tomd/ui.py`, classe `DropZone`:

1. Junto a `files_dropped = Signal(list)`, adicionar:
   ```python
   url_dropped = Signal(str)
   ```
2. Adicionar o método:
   ```python
   def _split_payload(self, mime_data):
       """Separa o que foi arrastado em arquivos locais suportados e links
       http(s). Navegadores fornecem a URL como QUrl remoto; alguns também
       só como texto — o fallback de texto cobre esse caso."""
       files, links = [], []
       for url in mime_data.urls():
           local = url.toLocalFile()
           if local:
               if is_supported_file(local):
                   files.append(local)
           elif url.scheme() in ("http", "https"):
               links.append(url.toString())
       if not links and not files and mime_data.hasText() and is_url(mime_data.text()):
           links.append(mime_data.text().strip())
       return files, links
   ```
3. Substituir `dragEnterEvent` e `dropEvent`:
   ```python
   def dragEnterEvent(self, event: QDragEnterEvent):
       files, links = self._split_payload(event.mimeData())
       if files or links:
           event.acceptProposedAction()
           self.set_active_style()

   def dropEvent(self, event: QDropEvent):
       self.reset_style()
       event.acceptProposedAction()
       files, links = self._split_payload(event.mimeData())
       if files:
           self.files_dropped.emit(files)
       elif links:
           self.url_dropped.emit(links[0])
       else:
           QMessageBox.warning(
               self,
               "Formato não suportado",
               "Esse tipo de arquivo não é reconhecido pelo conversor.\n"
               "Veja a lista de formatos aceitos em \"Escolher arquivo\"."
           )
   ```
4. Adicionar `is_url` ao import de `tomd.web` em `tomd/ui.py` (já está importado desde a task 6 — conferir).

- [ ] **Passo 2: Conectar na MainWindow**

Em `init_ui`, logo após a conexão de `files_dropped`:

```python
        self.drop_zone.url_dropped.connect(self.start_conversion)
```

Atualizar também o texto do card: `self.text_label = QLabel("Arraste um documento aqui")` vira

```python
        self.text_label = QLabel("Arraste documentos ou um link aqui")
```

- [ ] **Passo 3: Verificar e commitar**

Run: `python3 -m py_compile tomd/ui.py && python3 -m pytest tests/ -q`
Expected: sem erro; todos passam.

```bash
git add tomd/ui.py
git commit -m "Área de drop aceita links http(s) arrastados do navegador"
```

> ⚠️ **Checkpoint da Fase 2:** pedir ao usuário um smoke test no Windows (ou adiar para a task 16): arrastar 1 arquivo, 3 arquivos, 1 link; cancelar no meio do lote; provocar um erro (arquivo corrompido) e conferir a mensagem + log em `%LOCALAPPDATA%\toMD\logs\tomd.log`.

---

## Fase 3 — Licença e repositório público

### Task 12: LICENSE.md + README (licença, SmartScreen, formatos)

**Files:**
- Create: `LICENSE.md`
- Modify: `README.md`

- [ ] **Passo 1: Baixar o texto oficial da PolyForm Noncommercial 1.0.0**

```bash
curl -fsSL -o LICENSE.md https://raw.githubusercontent.com/polyformproject/polyform-licenses/1.0.0/PolyForm-Noncommercial-1.0.0.md
grep -q "PolyForm Noncommercial License 1.0.0" LICENSE.md && echo OK
```

Expected: `OK`. (URL verificada em 2026-07-13; se falhar, copiar o texto integral de <https://polyformproject.org/licenses/noncommercial/1.0.0/>.)

- [ ] **Passo 2: Adicionar o aviso de copyright no topo**

Inserir estas duas linhas no início de `LICENSE.md`, antes do título (formato de "Required Notice" que a própria licença especifica na seção Notices):

```markdown
Required Notice: Copyright © 2026 Mateus Freitas Gonçalves

```

- [ ] **Passo 3: Atualizar o README**

1. Adicionar, antes da seção "💡 Ideias para o futuro":
   ```markdown
   ## 📜 Licença

   O to.MD é gratuito para uso pessoal e não comercial — use, adapte e compartilhe à vontade. O que **não** pode é usá-lo (ou código derivado dele) para ganhar dinheiro. Texto completo: [LICENSE.md](LICENSE.md) (PolyForm Noncommercial 1.0.0). Copyright © 2026 Mateus Freitas Gonçalves.
   ```
2. Na seção "Opção 1 — Instalador", adicionar após o passo 1:
   ```markdown
   > 🛡️ O Windows pode mostrar um aviso do SmartScreen ("aplicativo não reconhecido") porque o instalador não tem assinatura digital paga. Clique em **Mais informações → Executar assim mesmo** — o app roda 100% na sua máquina e nenhum documento sai dela.
   ```
3. Na tabela "Formatos suportados", **remover** a linha `| 🎵 Áudio / Vídeo | ... |`.
4. Na seção "💡 Ideias para o futuro", remover a linha "Conversão em lote (vários arquivos de uma vez)" (já implementada).

- [ ] **Passo 4: Verificar e commitar**

Run: `grep -c "Required Notice" LICENSE.md && grep -c "Licença" README.md`
Expected: ambos ≥ 1.

```bash
git add LICENSE.md README.md
git commit -m "Licença PolyForm Noncommercial 1.0.0 e README com licença e aviso do SmartScreen"
```

---

### Task 13: Tornar o repositório público ⚠️ CHECKPOINT COM O USUÁRIO

**Ação irreversível na prática (o histórico fica exposto). NÃO executar sem confirmação explícita do usuário nesta sessão.**

- [ ] **Passo 1: Pré-checagens**

```bash
git log --oneline | head -20
git ls-files | grep -ivE "\.(py|md|toml|iss|spec|ico|yml|gitignore)$|^\.gitignore$|^run\.py$" || echo "nenhum arquivo inesperado"
```

Confirmar: LICENSE.md commitado e enviado ao `main`; nenhum documento pessoal no histórico (a spec registra que o histórico está limpo — conferir mesmo assim a lista de arquivos de cada commit com `git log --stat`).

- [ ] **Passo 2: Perguntar ao usuário e, com o OK, publicar**

```bash
git push origin main
gh repo edit ludimila/toMD --visibility public --accept-visibility-change-consequences
```

Expected: `gh repo view ludimila/toMD --json visibility` mostra `"visibility": "PUBLIC"`.

---

## Fase 4 — Automação de build (GitHub Actions)

### Task 14: `.github/workflows/test.yml` — testes em cada push/PR

**Files:**
- Create: `.github/workflows/test.yml`

- [ ] **Passo 1: Criar o workflow**

```yaml
name: Testes

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      # Só as dev-deps leves: os testes cobrem lógica pura e rodam sem
      # Docling/PySide6 (o conftest fornece um stub de QtCore).
      - name: Instala dependências de teste
        run: pip install pytest==9.1.1

      - name: Roda os testes
        run: pytest -v
```

- [ ] **Passo 2: Commitar, enviar e conferir no CI**

```bash
git add .github/workflows/test.yml
git commit -m "CI: roda os testes em cada push e pull request"
git push origin main
gh run watch --exit-status
```

Expected: run "Testes" verde (~1 min).

---

### Task 15: `release.yml` + versão do instalador via `/D`

**Files:**
- Create: `.github/workflows/release.yml`
- Modify: `installer.iss:6-8`

**Interfaces:**
- Consumes: `tomd/version.py.__version__` (extraída via `python -c`), `to.MD.spec` (aponta para `run.py` desde a task 6).
- Produces: em cada tag `v*`, um GitHub Release com `installer_output/to.MD_Setup.exe` anexado. **O workflow falha se a tag não bater com `tomd/version.py`.**

- [ ] **Passo 1: `installer.iss` aceita a versão de fora**

Trocar as linhas 6-8:

```iss
#define MyAppName "to.MD"
#define MyAppVersion "1.0"
#define MyAppExeName "to.MD.exe"
```

por:

```iss
#define MyAppName "to.MD"
; A versão vem do CI via /DMyAppVersion=<versão de tomd/version.py>;
; o default abaixo vale só para builds locais na mão.
#ifndef MyAppVersion
  #define MyAppVersion "1.1"
#endif
#define MyAppExeName "to.MD.exe"
```

- [ ] **Passo 2: Criar `.github/workflows/release.yml`**

```yaml
name: Release

on:
  push:
    tags: ["v*"]

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Confere se a tag bate com tomd/version.py
        shell: bash
        run: |
          APP_VERSION=$(python -c "exec(open('tomd/version.py').read()); print(__version__)")
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          if [ "$APP_VERSION" != "$TAG_VERSION" ]; then
            echo "::error::A tag $GITHUB_REF_NAME não bate com tomd/version.py ($APP_VERSION)."
            exit 1
          fi
          echo "APP_VERSION=$APP_VERSION" >> "$GITHUB_ENV"

      # --no-cache-dir: o torch é gigante e o disco do runner é apertado.
      - name: Instala as dependências completas
        run: pip install --no-cache-dir . pyinstaller==6.21.0

      - name: Gera o executável (PyInstaller)
        run: pyinstaller to.MD.spec

      # O Inno Setup vem pré-instalado na imagem windows-latest.
      - name: Gera o instalador (Inno Setup)
        shell: pwsh
        run: |
          & "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe" installer.iss "/DMyAppVersion=$env:APP_VERSION"

      - name: Publica o GitHub Release com o instalador
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release create "$GITHUB_REF_NAME" "installer_output/to.MD_Setup.exe" \
            --title "to.MD $GITHUB_REF_NAME" --generate-notes
```

- [ ] **Passo 3: Commitar e enviar**

```bash
git add .github/workflows/release.yml installer.iss
git commit -m "CI: gera e publica o instalador Windows em cada tag v*"
git push origin main
```

Expected: push OK (o workflow só dispara com tag — validação na task 16).

---

### Task 16: Tag de teste e validação do instalador ⚠️ CHECKPOINT COM O USUÁRIO

O objetivo é validar o `release.yml` **antes** de anunciar qualquer coisa. O updater (Fase 5) ainda não existe, então este release é descartável: valida, apaga, e o `v1.1` definitivo sai na task 19.

- [ ] **Passo 1: Criar e enviar a tag de teste**

```bash
git tag v1.1
git push origin v1.1
gh run watch --exit-status
```

Expected: workflow "Release" verde. Riscos conhecidos na primeira execução: **disco** (torch é gigante — se faltar espaço, adicionar um passo inicial que apague caches da imagem, p.ex. `Remove-Item -Recurse -Force C:\hostedtoolcache\CodeQL`) e **tempo** (PyInstaller com torch demora; é normal passar de 30 min).

- [ ] **Passo 2: Validar com o usuário no Windows**

Baixar o `to.MD_Setup.exe` do release e pedir ao usuário para instalar e conferir:
1. app abre e converte um PDF;
2. **o progresso por página funciona** (o monkeypatch depende de internals do docling; a versão pinada `2.112.0` pode divergir da usada no build anterior — se o progresso não aparecer, ajustar `_load_docling` em `tomd/engine.py` para a API atual do docling antes de seguir);
3. lote de 3 arquivos, link arrastado, mensagem de erro amigável, log criado.

- [ ] **Passo 3: Apagar o release de teste**

```bash
gh release delete v1.1 --yes
git push --delete origin v1.1
git tag -d v1.1
```

Expected: página de releases vazia de novo — o `v1.1` definitivo sai na task 19, com o updater incluído.

---

## Fase 5 — Aviso de nova versão

### Task 17: `tomd/updates.py` — checagem de release

**Files:**
- Create: `tomd/updates.py`
- Test: `tests/test_updates.py`

**Interfaces:**
- Consumes: `tomd.version.__version__`; stub de QtCore do conftest.
- Produces: `parse_version(text: str) -> tuple`, `is_newer(latest: str, current: str) -> bool`, `RELEASES_PAGE_URL: str`, `UpdateCheckThread(QThread)` com sinal `update_available(str, str)` (tag, url da página do release). Toda falha é silenciosa (só log).

- [ ] **Passo 1: Escrever os testes que falham**

Criar `tests/test_updates.py`:

```python
from tomd.updates import is_newer, parse_version


def test_ordenacao_numerica_nao_lexicografica():
    assert is_newer("1.1", "1.0")
    assert is_newer("1.10", "1.1")
    assert not is_newer("1.1", "1.10")
    assert is_newer("2.0", "1.10")


def test_ignora_prefixo_v():
    assert is_newer("v1.2", "1.1")
    assert parse_version("v1.2") == (1, 2)
    assert parse_version("V1.2") == (1, 2)


def test_igual_nao_e_mais_nova():
    assert not is_newer("1.1", "1.1")
    assert not is_newer("v1.1", "1.1")
    assert not is_newer("1.1.0", "1.1")


def test_tag_invalida_e_ignorada():
    assert not is_newer("beta", "1.1")
    assert not is_newer("", "1.1")
```

- [ ] **Passo 2: Rodar para ver falhar**

Run: `python3 -m pytest tests/test_updates.py -v`
Expected: ERROR `ModuleNotFoundError: No module named 'tomd.updates'`

- [ ] **Passo 3: Criar `tomd/updates.py`**

```python
"""Aviso de nova versão: consulta a API pública de releases do GitHub no
startup. Sem nag e sem auto-update — o app apenas mostra uma barra discreta
com um botão que abre a página de download no navegador. Qualquer falha
(sem rede, rate limit, JSON inesperado) é silenciosa: vai para o log e nada
aparece na tela."""
import logging
import re

from PySide6.QtCore import QThread, Signal

from tomd.version import __version__

RELEASES_API_URL = "https://api.github.com/repos/ludimila/toMD/releases/latest"
RELEASES_PAGE_URL = "https://github.com/ludimila/toMD/releases/latest"

log = logging.getLogger(__name__)


def parse_version(text: str) -> tuple:
    """"v1.10" -> (1, 10). Partes não numéricas encerram a leitura;
    uma tag sem número algum vira tupla vazia (tratada como inválida)."""
    text = text.strip().lstrip("vV")
    parts = []
    for piece in text.split("."):
        match = re.match(r"\d+", piece)
        if not match:
            break
        parts.append(int(match.group()))
    return tuple(parts)


def is_newer(latest: str, current: str) -> bool:
    lt, ct = parse_version(latest), parse_version(current)
    if not lt:
        return False
    # Iguala os comprimentos para que "1.1" == "1.1.0".
    size = max(len(lt), len(ct))
    lt += (0,) * (size - len(lt))
    ct += (0,) * (size - len(ct))
    return lt > ct


class UpdateCheckThread(QThread):
    update_available = Signal(str, str)  # (tag, url da página do release)

    def run(self):
        try:
            import requests  # import preguiçoso: os testes rodam sem requests

            response = requests.get(RELEASES_API_URL, timeout=5)
            response.raise_for_status()
            data = response.json()
            tag = data.get("tag_name", "")
            url = data.get("html_url") or RELEASES_PAGE_URL
            if is_newer(tag, __version__):
                self.update_available.emit(tag, url)
        except Exception:
            log.warning("Checagem de nova versão falhou (ignorada)", exc_info=True)
```

- [ ] **Passo 4: Rodar para ver passar**

Run: `python3 -m pytest tests/ -v`
Expected: todos passam.

- [ ] **Passo 5: Commit**

```bash
git add tomd/updates.py tests/test_updates.py
git commit -m "Checagem de nova versão via API de releases do GitHub"
```

---

### Task 18: Barra de aviso na janela principal

**Files:**
- Modify: `tomd/ui.py` (MainWindow: barra no topo + thread de checagem)

**Interfaces:**
- Consumes: `tomd.updates.UpdateCheckThread` (sinal `update_available(str, str)`), `tomd.updates.RELEASES_PAGE_URL`.

- [ ] **Passo 1: Adicionar imports**

Em `tomd/ui.py`: adicionar `QUrl` ao import de `PySide6.QtCore`, `QDesktopServices` ao import de `PySide6.QtGui`, e:

```python
from tomd.updates import RELEASES_PAGE_URL, UpdateCheckThread
```

- [ ] **Passo 2: Criar a barra (escondida) no `init_ui`**

Logo após `main_layout.setSpacing(20)` e antes do cabeçalho:

```python
        # Barra discreta de nova versão: só aparece se o updater sinalizar.
        # Não bloqueia nada e não repete diálogos — sem nag.
        self.update_bar = QWidget()
        self.update_bar.setObjectName("UpdateBar")
        self.update_bar.setStyleSheet(f"""
            #UpdateBar {{
                background-color: {SURFACE};
                border: 1px solid {HAIRLINE};
                border-radius: 8px;
            }}
        """)
        bar_layout = QHBoxLayout(self.update_bar)
        bar_layout.setContentsMargins(12, 6, 12, 6)
        bar_layout.setSpacing(8)

        self.update_label = QLabel("Nova versão disponível")
        self.update_label.setFont(make_font(12))
        self.update_label.setStyleSheet(f"color: {INK}; border: none; background: transparent;")
        bar_layout.addWidget(self.update_label, 1)

        self.update_button = QPushButton("Baixar")
        self.update_button.setObjectName("primaryButton")
        self.update_button.setFont(make_font(12, QFont.DemiBold))
        self.update_button.setCursor(Qt.PointingHandCursor)
        self.update_button.setFixedSize(84, 30)
        self._update_url = RELEASES_PAGE_URL
        self.update_button.clicked.connect(
            lambda: QDesktopServices.openUrl(QUrl(self._update_url))
        )
        bar_layout.addWidget(self.update_button)

        main_layout.addWidget(self.update_bar)
        self.update_bar.hide()
```

(`QFont` já está no import de `PySide6.QtGui` desde a task 6.)

- [ ] **Passo 3: Disparar a checagem no `__init__`**

No final de `MainWindow.__init__`, após `self.init_ui()`:

```python
        self._update_checker = UpdateCheckThread(self)
        self._update_checker.update_available.connect(self._show_update_bar)
        self._update_checker.start()
```

E adicionar o método:

```python
    @Slot(str, str)
    def _show_update_bar(self, tag, url):
        self._update_url = url
        self.update_label.setText(f"Nova versão disponível ({tag})")
        self.update_bar.show()
```

- [ ] **Passo 4: Verificar e commitar**

Run: `python3 -m py_compile tomd/ui.py && python3 -m pytest tests/ -q`
Expected: sem erro; todos passam.

```bash
git add tomd/ui.py
git commit -m "Barra de aviso de nova versão com botão que abre a página de download"
```

---

### Task 19: Release final v1.1 ⚠️ CHECKPOINT COM O USUÁRIO

- [ ] **Passo 1: Conferir a versão e enviar tudo**

```bash
grep '__version__' tomd/version.py   # deve mostrar "1.1"
git push origin main
gh run watch --exit-status           # workflow "Testes" verde
```

- [ ] **Passo 2: Tag definitiva**

```bash
git tag v1.1
git push origin v1.1
gh run watch --exit-status
```

Expected: workflow "Release" verde; release `v1.1` publicado com `to.MD_Setup.exe`.

- [ ] **Passo 3: Validação final com o usuário no Windows**

1. Instalar a partir do release e repetir o smoke test da task 16.
2. Conferir o updater de ponta a ponta: instalar uma build antiga (ou trocar temporariamente `__version__` local para `"1.0"` e rodar `python run.py`) e verificar que a barra "Nova versão disponível (v1.1)" aparece e o botão abre a página do release.
3. Avisar os usuários (fora do escopo deste plano).

---

## Notas de risco (da spec, para quem executa)

- **Monkeypatch de progresso** (`_load_docling` em `tomd/engine.py`) depende de `docling.pipeline.standard_pdf_pipeline.ProcessingResult`. A versão pinada `docling==2.112.0` foi escolhida por ser a mais recente no PyPI em 2026-07-13, mas **não é necessariamente a versão do último build funcional** — a task 16 valida o progresso por página no Windows antes de qualquer anúncio. Se quebrar, o conserto acontece em `_load_docling`, nunca soltando a versão pinada.
- **Disco no runner Windows**: torch + PyInstaller podem estourar o espaço. Mitigação na task 16.
- **SmartScreen** continuará avisando (sem assinatura de código) — coberto no README (task 12).
